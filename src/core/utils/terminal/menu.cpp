#include <menuItem.hpp>
#include <subMenu.hpp>

namespace core:utils
{
    class Menu
    {
            Menu::Menu(menuItem[itemCount] items) : items(items)
            {
                
            }
    }
}